# Empty set
emptyset = set() # we use this becouse s = {} this means empty dictionary

# set print unordered way
# if there are duplicate element in set then on printing time duplicate values will not print
s = {1, 5, 32, 53, 5, 12, 5, 53}
print(s) 


#properties of set
# 1. Sets are unordered => Element’s order doesn’t matter
# 2. Sets are unindexed => Cannot access elements by index
# 3. There is no way to change items in sets.
# 4. Sets cannot contain duplicate values.